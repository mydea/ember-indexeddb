'use strict';

module.exports = {
  description: 'Setup necessary files for ember-indexeddb',
  normalizeEntityName() {
    // leave empty for main blueprint run by `ember install ember-indexeddb`
  },
};
